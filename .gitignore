# 白名单模式：先把整个 skills 目录全部忽略……
/*

# ……再逐个放行属于 ai-image-skills 套装的东西
!.gitignore
!README.md
!CHANGELOG.md
!_registry/
!bg-remover/
!batch-image-resize/
!batch-style-transfer/
!image-element-crop/
!head-shoulder-crop/
!lucky-item-style-transfer/

# 这样做的目的：~/.codebuddy/skills/ 里还装着大量第三方 skill
#（腾讯文档、企业微信套件、小红书、follow-builders、westock-partner 等），
# 它们不属于本套装，绝不能被提交上去。
#
# ⚠️ 注意：本目录已是git 仓库，但里面有大量「被忽略」的第三方 skill。
#    永远不要在这里执行 git clean -fdx —— 它会把那些第三方 skill 一起删掉。

# ===== 以下是放行目录内部仍要排除的东西 =====

# 密钥 / 私密配置：绝不能提交
TOOLS.md
**/TOOLS.md
.env
**/.env
*.env
*.key
**/*credential*
**/*secret*

# 台账含「已分发给谁」等私人信息，只留本地
_registry/工作流台账.md

# 各人本机的登录态 / 个人配置（本来就在 home 下，这里再保一道）
**/.lucky-item-gemini-profile/

# 运行产物
**/output/
**/output_cropped/
**/raw_images/
**/AI-幸运物待处理/
**/输出-幸运物/
**/dist/
*.zip

# 系统与编辑器垃圾
**/.DS_Store
**/.vscode/
**/.idea/
*.swp
*.swo
*.log

# Python
**/__pycache__/
*.py[cod]
*$py.class
*.so
**/.venv/
**/venv/
