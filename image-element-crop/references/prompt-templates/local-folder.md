# 本地文件夹批量模式示例

以下仅为已填写的参考示例；缺少参数时请使用同目录的 `参数申领单.txt`。

```markdown
请用 image-element-crop skill 批量裁剪图片，以下为我已确认的参数。

【必填】
输入来源：本地文件夹
输入路径或表格链接：./photos
目标尺寸（宽x高，可逗号分隔多个）：800x800
输出目录：./output

【选填】
描述文件（仅本地文件夹模式）：descriptions.json
裁剪策略：contain（保留完整元素）
留白百分比：5
是否人工校验：默认关闭，直接出成品
同名文件处理：自动加序号 -2/-3
```
